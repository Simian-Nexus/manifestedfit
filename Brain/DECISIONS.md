# Decisions

## 2026-05-01

- Use a shared-hosting-friendly static/PHP funnel as the first working build.
- Keep FTP credentials in ignored `config.json` files inside intent-named target folders under `07_Deploy/targets/`.
- Use the existing Manifested Fit logo from `Media/logo.png` as the first visual anchor.
- Treat Mindvalley as the initial aspirational affiliate target, but do not depend on approval for the site launch path.
- Keep archived old site/content material untouched unless there is a specific reason to migrate it.
