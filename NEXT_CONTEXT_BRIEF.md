# Next Context Brief

**Last updated: 2026-07-12.** Point a fresh session at this file to continue seamlessly.

## Session prep 2026-07-12 (by Jeeves — distilled from this brief + Brain/TASKS.md; no state changed)

Jonathan plans to work on this project imminently. Highest-value next moves, in order:

1. **Run one fresh post through the local-Wan hybrid backend** and inspect
   `visual_provenance.json` + quality (expect up to 2 `actual: local_wan` beats,
   ~8 min warm). This is the top unvalidated piece of the pipeline.
2. **Veo billing decision**: Veo returns 429 RESOURCE_EXHAUSTED until the Gemini
   API project is on a paid tier with balance. Decide pay vs. stay on local Wan + Pexels.
3. **Google Search Console + GA4** were deliberately deferred to ~the week of
   **2026-07-13 — that's now.** Set both up, then re-run Rank Math's Analytics connect.
4. Smaller Jonathan-only items still open per the 07-09/07-11 notes: activate the
   Affiliates plugin in wp-admin + enter real offers; paste the Pexels key into
   Content Engine settings (and consider rotating it — it echoed into a session log
   2026-07-09); Dana Cole's music folder is still empty; confirm the Bluehost cPanel
   cron exists; topic queue may need ~2 weeks of topics (was empty per TASKS.md);
   trash "Pipeline Test" drafts 28/29; the FTP-script permission entry in
   `.claude/settings.local.json` was never added (manual FileZilla is the fallback).

Unverified since 2026-07-11: remote queue state, cron existence, topic queue depth —
check live before acting.

## Post #73 hybrid video — completed

- Post #73, **“Why You Feel Tired All the Time (And Small Fixes That Help)”** (Dana Cole), now has public, embedded YouTube video `PA61WiNhIYk`. Telegram approval completed and the remote video queue is empty. Do not regenerate it.
- Hybrid API correction is local and verified: planner/QA = `gemini-3.5-flash`; Veo = `veo-3.1-fast-generate-preview`; live preflight printed `planner/QA + Veo ready`.
- Dashboard security correction is local and verified: `/api/state` no longer contains `gemini_api_key`; it returns only `gemini_api_key_set`. Blank key input preserves the stored secret.
- Start the dashboard via `07_Deploy/targets/video-worker/run_dashboard.bat` if UI control is wanted; no diagnostic dashboard process was intentionally left running.
- **Resumable generation added and tested**: per-post state lives in ignored `video-worker/work/post_<ID>/`; an in-flight Veo operation is re-polled rather than resubmitted; completed media and YouTube preview IDs are reused. Dashboard has **Fresh restart** and CLI has `--fresh`, which should be used only for intentional regeneration/rejection/input changes. Full protocol: `07_Deploy/targets/video-worker/RESUMABLE_GENERATION.md`.
- **Veo billing blocker proven with post #77**: hybrid selected 5 Veo beats, but Google returned `429 RESOURCE_EXHAUSTED` with a plan/billing/quota message on every submit; final provenance was 0 Veo + 11 Pexels. Video `rL4vWim7y0M` is public and embedded. Enable paid billing / positive balance for this API project before another Veo test. Worker now records actual per-beat sources in `visual_provenance.json` and stops retrying Veo after the first 429.
- **Free local Wan backend installed/integrated**: ComfyUI + Wan 2.1 T2V 1.3B runs on RTX 3060 12 GB. Benchmarks: 3.06s/12 steps = 3m33s; 3.06s/20 steps = 4m03s, preferred quality; ~6.1 GB VRAM. Hybrid config now defaults to local Wan, 20 steps, max 2 clips/video (~8 min warm), with Pexels for remaining beats. Worker auto-starts private ComfyUI with paid API nodes disabled and slows raw clips to beat duration. Next: test one real fresh post and inspect provenance/quality. See `video-worker/local-video/README.md`.

## Progress 2026-07-10 (this session) — news rewrites, blog nav link, video polish

- **Static funnel now links to the blog**: `03_Website/public/index.html` header nav + footer ("Blog" -> `blog/`). Needs the usual FTP push of index.html (Bluehost 451 workaround applies).
- **News-rewrite engine built (plugin v0.7.0 + theme v2.1.0, NOT yet deployed to Bluehost)**: theme's `[mf_wellness_news]` fires `mf_wellness_news_fetched` after each fresh RSS pull; the plugin queues unseen headlines (`mfce_news_queue` option) and a one-off wp-cron event rewrites up to N/day (setting `news_max_per_day`, default 2; toggle `news_rewrite`, default on) into ORIGINAL news-reaction DRAFTS: persona-of-the-day byline, "Wellness News" category + `news` tag, source fetched best-effort for grounding, guaranteed linked citation appended, Pexels featured image (never publisher photos — citation is not a license), normal Telegram Publish/Keep/Trash approval, no video pipeline. Once a rewrite is PUBLISHED, the front-page news card links to OUR post (styled `.mf-news-card-ours`, "Our take") instead of the external article — nothing is hidden, the card is upgraded. `php -l` passed on both files; **mirror verification still to do** (Docker Desktop wasn't running) before pushing theme + plugin to Bluehost via FileZilla (the FTP-script permission from 2026-07-08 was never added to `.claude/settings.local.json`).
- **Video worker upgrades (all verified with a real local test render)**:
  - Intro is now **8s** (was 3s) with glow + floating logo, staggered title/tagline, jingle trimmed/faded to fit.
  - **Animated end card** `branding/endcard.mp4`: cursor slides in, clicks SUBSCRIBE (pressed state), slides to a golden bell and clicks it, bell swings; a generated `branding/ding.mp3` ("ding ding") is mixed by the worker at outro-start + 2.0s so sound matches the swing. Static `endcard.png` kept as fallback.
  - **End card holds ~5s after the spoken outro** (`endcard_hold_seconds`, default 5, in config + dashboard passthrough) instead of cutting at voice-end; audio is padded and faded to match.
  - **B-roll is now beat-aligned**: the narration is split into ~4.5s+ sentence beats, each beat gets its own Pexels search from its own words and is cut to that beat's estimated speech time (same word-share timing as the captions), so visuals follow what's being said. True per-word sync would need forced alignment (edge-tts word boundaries) or generative video — Jonathan flagged agent.opus.pro (Nano Banana Pro + Seedance) as the model; our beat segmentation is the layer a generative backend could plug into later.
  - Dashboard asset checklist now reports endcard.mp4/ding.mp3; `build_branding.py` regenerates everything (bell/cursor/button sprites drawn with Pillow from the venv).
- **Hybrid visuals + narration-sync layer added** (later same session, all local, verified with test render): (1) `visuals_engine` now supports **hybrid** (his config is set to it) — Gemini plans per beat whether Veo-generated or Pexels stock fits better (cap still applies), with a tailored Veo prompt / Pexels query each; (2) **AI frame check** (`visual_qa` setting, on): every proposed clip — Pexels candidates' preview frames AND generated Veo clips' middle frame — is scored by Gemini vision against the exact narration line for that timestamp; bad fits are rejected (Pexels: next candidate/query; Veo: falls back to stock); (3) **exact narration timing**: edge-tts 7.x `SentenceBoundary` stream events give real spoken sentence start/end times — beats AND burned captions now use true timing, not word-count estimates (chatterbox/CLI degrade gracefully to estimates). QA model setting `gemini_qa_model` (default gemini-2.5-flash). Planner/QA/Veo paths still untested against the live API — **Gemini key still not pasted**.
- ~~Not deployed yet~~ **DEPLOYED**: Jonathan uploaded plugin v0.7.0 + theme v2.1.0 + index.html himself; verified via FTP hash-compare (all three remote files match local). News engine goes live on the next fresh RSS pull.
- **Google Veo visuals engine added to the worker** (same session): `visuals_engine` setting (`pexels` default | `veo`) — with `veo`, each narration beat's clip is GENERATED from that beat's spoken text via the Gemini API (`veo_generate_clip`: predictLongRunning -> poll -> download; defensive parsing across Veo response shapes). `veo_max_clips_per_video` (default 8) caps credit burn; over-cap beats and any Veo failure fall back to Pexels per beat. Key goes in worker `config.json` `gemini_api_key` or the dashboard's new "Gemini API key (Veo)" field (General settings, alongside engine dropdown/model/cap). Default model `veo-3.0-fast-generate-001`; Veo 3.1 models get `durationSeconds` 4/6/8 matched to the beat. Veo's own audio is discarded (clips normalized with `-an`). **Jonathan still needs to paste the Gemini key + flip the dropdown to Veo.** Untested against the real API until a key exists.

## Progress 2026-07-09 (this session) — blog redesign live, affiliates plugin, engine v0.6.0

- **Blog redesigned and LIVE on Bluehost**: child theme v2.0.0 — magazine front page (`templates/home.html`): hero slider with pagination dots/arrows/autoplay (`assets/mf-blog.js`), "The Latest" card grid, **Wellness News** section (merged Google News RSS via `fetch_feed`, 3h transient, new-tab links), CTA band, dark footer; branded sticky header/footer via `[mf_header]`/`[mf_footer]` shortcodes (real logo from `Media/logo.png`, resized into theme `assets/logo.png`). Fraunces display serif + Inter via Google Fonts.
- **Local Docker mirror** at `03_Website/wordpress/local-mirror/` → http://localhost:8090 (admin/admin), theme + both plugins bind-mounted, live content imported. **Workflow rule from Jonathan: change the mirror first, verify, then push to Bluehost.** Gotchas in auto-memory: `MSYS_NO_PATHCONV=1` for docker/wp-cli in Git Bash; mirror-only `mu-plugins/local-ssl.php` for TLS interception.
- **Bluehost FTP deploys work now**: `--ssl-no-revoke` added to `publish-ftp-files.ps1`; Bluehost 451-rejects some uploads (all `.html`, sometimes big `.php`) → upload as `upload.tmp` + `RNFR/RNTO` rename (see memory + scratchpad `upload_plugins.ps1` pattern). Derived git-ignored configs: `07_Deploy/targets/wp-theme/config.json`, `wp-plugins/config.json`.
- **NEW plugin `manifested-fit-affiliates` v0.1.0** (deployed, **needs activation in wp-admin**): categorized offer registry ("Affiliate Ads" admin page), AI-decided in-post "Partner pick" ad cards via `mfce_draft_created` hook + `MFCE_Engine::ai_json` (falls back to even spacing), no active offers = no ads, `rel="sponsored nofollow"`, disclosure line, re-plan button (`all` supported). No offers entered yet.
- **Content engine v0.6.0 deployed**: (1) Pexels featured image on each new draft (new setting `pexels_api_key` — **Jonathan still needs to paste the key**); (2) front end hides `[VIDEO EMBED]` and, with a key, swaps in a topical Pexels **stand-in image** (meta `mfce_standin_img`) until the real video embeds; (3) **Run Now no longer consumes the daily cron slot** (only `*-cron` contexts stamp `last_run_date`) — this was why 2026-07-09's cron skipped: the evening Run Now landed past midnight server time (server = UTC); (4) queue low (≤2 pending) → Telegram sends 4 AI topic suggestions with one-tap **Add** buttons (`addtopic:` callback, `mfce_tg_suggestions` option, once/day); (5) Telegram AI chat is now self-aware (knows queue, recent posts, personas, schedule) and queues topics via `ADD_TOPIC: title | notes` directive lines; (6) draft notices show "Publish w/o video" + warning when the video isn't made yet.
- **YouTube embeddable bug found & fixed**: `videos().update(part="status")` replaces the whole status object, so omitting `embeddable` disabled embedding ("playback restricted"). Worker patched (insert + make_public now send `embeddable: True`); both live videos repaired. Post 32's video was also still **unlisted and never embedded** — flipped public + re-embedded via the `video-embed` endpoint (worker config holds `mfce_cron_secret`).
- **Featured images backfilled** on all live posts (Pexels, via REST). Music: 4 tracks each for Frankie/Nadia/Rowan in `music/<Persona>/`; **Dana Cole's folder is still empty**. `music_volume` 0.09 (quiet under voiceover). Jingle (`branding/jingle.mp3`, from Suno `Media/IntroJingle.mp3`) baked into `intro.mp4`.
- **Still on Jonathan**: activate Affiliates plugin; paste Pexels key into Content Engine settings; add real affiliate offers; Dana music; confirm/create the cPanel cron; consider rotating the Pexels key (it briefly echoed into a session log 2026-07-09); music/outro prompts live in `06_Planning/MUSIC_AND_OUTRO_BRIEF.md`.

## Progress 2026-07-08 (this session) — video pipeline built end-to-end

The video worker described below as "not built yet" now exists and has produced two real, embedded videos. Read this section first; it supersedes anything below that contradicts it.

- **YouTube OAuth is live.** Google Cloud project "Manifested Fit" created, YouTube Data API v3 enabled, OAuth consent screen **published** (out of Testing — refresh token no longer expires weekly). Desktop OAuth client + `client_secret.json` + `youtube_token.json` (both git-ignored) live in `07_Deploy/targets/video-worker/`. Channel uploads are currently **unlisted-then-public via API**; the free YouTube API audit to lift the new-project private/locked restriction has **not** been requested yet — worth doing early if uploads ever come back locked.
- **`video_worker.py` built and proven**: polls `mfce/v1/video-queue` → renders (TTS voiceover + Pexels b-roll + burned captions + branded intro + persona-worded spoken "like and subscribe" outro over a brand end-card + optional background music) → uploads unlisted to YouTube + uploads an accurate caption track (fixes YouTube's sloppy auto-captions) → posts `video-ready` (Telegram Approve/Reject) → on approval, flips public and calls `video-embed`. `--watch` mode waits for the Telegram decision itself. Run via `run_video_worker.bat` or (preferred) the dashboard.
- **Two videos are live and embedded**: post 33 (Nadia, edge-tts) and post 32 (Dana; post 32 predates brief-writing so a one-off brief was hand-written — see `brief_post32.json` / `backfill_post32.py`, safe to delete once no longer needed as a reference).
- **Chatterbox (local, open-source, ElevenLabs-alternative) TTS installed and working on the RTX 3060** (`cuda: True`, torch 2.6.0+cu124) — sample generated but Jonathan didn't like the default male voice. Chatterbox has no voice catalog; it only does **reference-clip cloning** (drop a ~10–20s WAV into `07_Deploy/targets/video-worker/refs/`). No ref clips exist yet — next step is finding/recording per-persona reference voices, or just staying on edge-tts (has ~400 catalog voices, already wired, sounds "robotic" per Jonathan's first listen).
  - **Install gotcha for this machine**: plain `pip install chatterbox-tts` fails two ways here — Bluehost-style TLS interception breaks pip's cert verification, and the `antlr4-python3-runtime` source build fails under this box's uv-based Python (`_distutils_hack` assertion). Fix used: a **dedicated venv** at `07_Deploy/targets/video-worker/venv/` (git-ignored), installed via **`uv pip install --system-certs --python venv\Scripts\python.exe ...`**, not plain pip. All worker scripts should be run through this venv's python, not the system one.
- **Branding assets**: `branding/logo.png` (copied from the site logo), `branding/build_branding.py` generates `intro.mp4` (3s animated logo intro, silent until a jingle exists) and `endcard.png` (logo + Like/Subscribe + manifestedfit.com) — both git-ignored generated outputs, rebuildable any time via the dashboard's "Rebuild intro/endcard" button. **Jingle not yet made** — Jonathan is signing up for Suno; prompts for the jingle + 4 persona background-music tracks were given in-session (not filed anywhere else — see chat history or ask Jonathan). Once music/jingle files exist (`branding/jingle.mp3`, `music/<Persona Name>/*.mp3`, all git-ignored), rebuild branding once for the jingle to bake into the intro.
- **Config-driven, not hardcoded**: `07_Deploy/targets/video-worker/config.json` (git-ignored; `config.example.json` is the checked-in template) holds `tts_engine` (edge|chatterbox), per-persona `voices` (edge voice id + optional chatterbox ref_wav/exaggeration/cfg_weight), per-persona `outros` (the spoken like/subscribe line, worded to match each persona's voice), music volume, watermark, caption/SRT toggles, approval-wait timeout.
- **Dashboard built**: `dashboard.py` (Flask, in the worker venv) + `run_dashboard.bat` → `http://localhost:8765`, localhost-only. Replaces hand-editing JSON/running .py/.bat files with a UI: live queue view, one-click "Run pipeline" (render→upload→wait for Telegram approval→publish+embed), a **voice sampler** (play any edge-tts catalog voice, or any Chatterbox ref clip, right in the browser) with per-persona voice/outro editing, general settings (engine, music volume, captions, watermark, approval wait) with Save, and an asset checklist (intro/endcard/jingle/music present or missing). This is now the primary way Jonathan should interact with the pipeline day to day.
- **Plugin fix (v0.4.2, live on Bluehost)**: the video embed used to insert a Gutenberg `wp:embed` (oEmbed) block, which failed to resolve on this hosting and left a bare YouTube URL visible on mobile (Jonathan caught this via screenshot). Replaced with a direct responsive `<iframe>` — no oEmbed dependency, works in any theme. `insert_video_markup()` also now knows how to replace either the legacy oEmbed block or a previous iframe, so re-running `video-embed` self-heals old posts. The video-queue endpoint now also returns each post's `persona` (author display name) so the worker can pick a matching voice.
- **FTP deploy permission**: Jonathan said yes to letting Claude run `07_Deploy/tools/publish-ftp-files.ps1` directly (no more manual FileZilla uploads for plugin/theme files), but the actual settings.json edit needs to be made by Jonathan himself — auto-mode blocks Claude from granting its own permissions. **Check whether `.claude/settings.local.json` has the `autoMode.allow` entry and the two `publish-ftp-files.ps1` permission rules from that conversation; if not, it's still pending and manual FileZilla uploads are the fallback.**
- **Not yet done**: Task Scheduler entry for daily automated runs (currently manual: double-click `run_video_worker.bat` or use the dashboard after the noon cron drafts a post); the free YouTube API audit request; jingle + persona music tracks; deciding on Chatterbox voice cloning vs. sticking with edge-tts; consistent persona portrait/avatar overlay (discussed as a good v2 idea — portrait card + name badge, not a lip-synced talking head).
- Everything from this session is committed and pushed (`76e2c2a feat(video): stand up the faceless video pipeline worker and control dashboard`). One item deliberately left uncommitted: `01_Admin/Manually_Copied_Context_Sessions/2026-07-06.docx` had local changes the auto-mode guard flagged as an unnamed sensitive-source file — ask Jonathan before committing that one.

## What this project is

Manifested Fit is a faceless AI wellness/manifestation brand with two income goals: YouTube ad revenue + affiliate commissions, supported by a blog on `manifestedfit.com`. The active workstream (July 2026) is **standing up an automated blog + content pipeline** with Claude as a supervised content engine. The strategy is fully written in `06_Planning/ACTION_PLAN_2026-07.md` (two lanes: Lane A voice-led is primary/money; Lane B stylized character is a test). Read that plan first.

## Current state (what's done and working)

- **WordPress blog is live at `manifestedfit.com/blog`** — installed in a subdirectory so the existing hand-built funnel at the site root is untouched. Runs on Bluehost.
- **Active theme:** Twenty Twenty-Five + a custom **brand child theme** we built: `03_Website/wordpress/themes/manifestedfit-blog/` (theme.json carries the brand palette teal `#08736f` / green / plum / cream + Inter; matches the flat site's `styles.css`). Installed via FileZilla, activated. SpeedyCache is active — clear it after changes. (Inter font not yet installed via Font Library; theme falls back to system sans. Claude offered to hard-wire Inter into the theme — not yet done.)
- **SEO:** Rank Math installed + wizard done. Google Search Console/Analytics deliberately deferred (see TASKS.md, ~week of 2026-07-13). **SiteSEO + SiteSEO Pro** (Softaculous installer defaults) are being removed as a duplicate-SEO-plugin conflict — deactivate SiteSEO Pro first, then SiteSEO.
- **Users:** `ContentEngine` (role **Editor**) is the API/posting user, with an Application Password created. Four persona/columnist authors exist (role Author): **DanaCole, FrankieMoon, NadiaBrooks, RowanEllis**.
- **The content pipeline works end-to-end.** `07_Deploy/tools/push-draft.ps1` (PowerShell, matches existing deploy tooling) reads git-ignored `07_Deploy/targets/wordpress/config.json` (site_url, username=ContentEngine, app_password), parses a Markdown file + front matter, converts to HTML, auto-creates categories by name, and **always posts as a draft** via the WP REST API. Proven working 2026-07-05 (test draft + categories created). Jonathan runs it locally; credentials never enter chat. Script is ASCII-only (an em-dash once broke Windows PowerShell 5.1 parsing — keep it ASCII).

## Progress 2026-07-06 (this session)

- **push-draft.ps1 fixed**: WP returns category names HTML-entity-encoded ("Rituals &amp;amp; Routines"), which broke the exact-name match and caused a 400 `term_exists` on re-create. Script now decodes entities and recovers the existing term id from the error body. Verified working (created drafts 28/29 "Pipeline Test" - **both should be trashed** along with any older test drafts).
- **Author IDs partially resolved via REST**: FrankieMoon = 4, RowanEllis = 5 (very likely - authored the 7-Day Reset page), ContentEngine = 6, admin = 1. Dana and Nadia are 2/3 in unknown order (Editor role can't list users). Moot for the plugin (resolves by login name), still needed for push-draft front-matter `author_id`.
- **Telegram wired end-to-end**: new bot **@ManifestedFitBot** ("Manifested Fit Engine"). Token + chat_id (6617208680) in git-ignored `07_Deploy/targets/telegram/config.json`. Test message sent successfully from PowerShell.
- **Content-engine WP plugin BUILT** (not yet uploaded/activated): `03_Website/wordpress/plugins/manifested-fit-content-engine/` - settings page (API key, Telegram, model default claude-opus-4-8), topic queue, persona schedule incl. holiday/solemn rules, Run Now button, secret-guarded REST cron endpoint + CLI cron-runner.php fallback, structured-JSON generation, RankMath focus keyword, drafts-only. See its README.md for install + Bluehost cron steps.
- **Two-way Telegram added to the plugin**: an "Enable two-way Telegram" button registers the bot webhook at `/wp-json/mfce/v1/telegram` (secret-token header verified, chat-id restricted, update_id deduped). Draft notices then carry Publish/Keep/Trash buttons (tapping Publish = the human approval; the engine never auto-publishes), replying to a notice makes the AI revise that draft, and any other message is answered by the configured model. Clarified for Jonathan: his claude.ai subscription CANNOT power the plugin/bot - server-side calls need the metered API key (already the plan).
- **Multi-provider support DONE (phase c)**: provider dropdown now offers Anthropic (default, claude-opus-4-8), Google Gemini (gemini-2.5-flash; Jonathan has a free-tier key - caveat: Google may train on free-tier API content), OpenAI (gpt-5.1), Grok/xAI (grok-4), and Custom/local OpenAI-compatible (e.g. Ollama on a VM - base URL + optional key + model; must be reachable FROM Bluehost, so a home box needs a tunnel like Tailscale Funnel/cloudflared). One structured-JSON pipeline for all; the custom provider degrades json_schema -> json_object -> prompted JSON for smaller local models. Provider drives generation, revision, and Telegram chat.
- **Remaining to go live**: Jonathan creates paid Anthropic API key (console.anthropic.com) - or flips provider to Gemini with his free key - uploads plugin via FTP, activates, pastes keys, tests Run Now, adds Bluehost cron.
- **Plugin IS live on the site** (later on 2026-07-06): Jonathan uploaded/activated it, set provider = Gemini (free tier), and two-way Telegram chat works. One historical `403 can't send messages to the bot` in the log means chat_id was briefly set to the bot's own id - now believed fixed (chat works). Topic queue is EMPTY and the Bluehost cPanel cron may not be created yet - both are needed before daily drafting starts.
- **Plugin v0.3.0 additions (same session)**: admin page reorganized into collapsible `<details>` sections (Status & actions / Engine & AI provider / Telegram / Cron & video endpoints / Topic queue / Recent activity); pending topics are editable in place and can pin a persona and/or provider+model per topic (falls back to default provider if the pinned one has no key); "AI topic fallback" setting (default on) - empty queue makes the AI pick today's topic from day-of-week/holiday/weekend/persona/recent-titles context, adds it to the queue flagged AI-picked, and notes it in the Telegram message. Jonathan's pasted topic list has stray rows (notes lines added as separate topics) - fix via the new inline editing. Bluehost cPanel cron still to be created (fields: minute 0, hour 6, day *, month *, weekday *, command = curl line from the plugin admin page).
- **Plugin v0.4.0 additions (same session)**: per-provider model DROPDOWNS (curated known ids + free-text override box that wins when filled; Anthropic list: claude-opus-4-8 default, claude-fable-5, claude-sonnet-5, 4.7/4.6/haiku); "Ask AI to plan the queue" (textarea for custom asks like seasonal week-long series) -> AI proposal stored in `mfce_topic_plan` option and rendered as a review MODAL with Apply (replaces pending topics, keeps used history) / Discard. Jonathan now HAS an Anthropic API key. His pasted topic list has stray rows (note lines became separate topics) - fix via inline edit or the AI planner. cPanel cron fields advised: minute 0, hour 6, rest *, command = the curl line from the plugin page (server timezone may differ from Pacific).
- **Plugin v0.2.0 - video pipeline (this session, built, NEEDS RE-UPLOAD via FTP)**: every non-solemn draft now carries a `<p>[VIDEO EMBED]</p>` placeholder (prompt-enforced) + an AI-written YouTube video brief in post meta (`mfce_video_brief`: yt title/description/voiceover script/visual direction; status meta `mfce_video_status`: needed→review→approved|rejected→embedded). Three new cron-secret-guarded REST endpoints for an external video worker: `GET /wp-json/mfce/v1/video-queue`, `POST /video-ready` (sends Telegram Approve video/Reject video buttons), `POST /video-embed` (swaps placeholder for a responsive YouTube embed block + Telegram notice with Publish buttons). Revise-by-reply now shields the placeholder/embed from the AI. Admin page lists the exact endpoint URLs. Workflow-side plan (video generation options, YouTube API setup, worker loop): `06_Planning/VIDEO_PIPELINE_PLAN.md` - recommended Option A = free local Python worker (edge-tts + Pexels + ffmpeg + YouTube Data API) on Task Scheduler; worker itself NOT built yet.

## The voice team + schedule (decided)

Four columnist personas, each a voice (spec + bios in `05_Content/blog/authors-and-schedule.md`):
- **Dana Cole** — grounded & practical. **Nadia Brooks** — warm & story-led. **Frankie Moon** — playful & good-vibes. **Rowan Ellis** — calm & minimal.

Posting schedule by day's mood: **Mon Frankie, Tue Dana, Wed Nadia, Thu Dana, Fri Rowan; Sat/Sun random.** Canadian statutory holidays → Frankie (happy vibe) — **except** National Day for Truth and Reconciliation (Sep 30) and Remembrance Day (Nov 11), which are solemn: use Rowan's calm voice or skip. Personas are voice bylines only — never fabricate credentials or use AI headshots-as-real-people; add an honest "About our voices" note.

## Content assets already written

- `05_Content/blog/post-01-morning-reset-ritual.md` — first post, original single-voice draft (with RankMath field values).
- `05_Content/blog/post-01-voice-variations.md` — the same post in all 4 voices (for the voice test).
- The **Golden Retriever** version (Frankie) is already a draft in WordPress, authored by FrankieMoon.
- `05_Content/lead-magnet/7-day-reset-content.md` — the written 7-Day Mind-Body Reset (Rowan voice); exists as a **draft page** in WP, needs gating behind email signup.
- `05_Content/blog/categories.md` — taxonomy: Manifestation, Mindset, Rituals & Routines, Gentle Movement, Sleep & Rest, Recipes.

## Open threads / next steps (see the task list)

1. **Finish removing SiteSEO/SiteSEO Pro**, trash the "Pipeline Test" draft.
2. **First real week of posts:** Claude writes Mon–Fri in the rotation; Jonathan pushes them. **BLOCKER:** needs the four author user IDs (Users → hover persona → `user_id=N`) to stamp bylines via front-matter `author_id`.
3. **Optimize post 1 in RankMath** (focus keyword, meta, slug, featured image) — needs a real embedded video first.
4. **Gate the 7-Day Reset page** behind email signup (GoSMTP is installed for delivery).
5. **The self-running engine (Jonathan's vision):** Bluehost cron → custom WP plugin → routes each scheduled topic to a chosen AI provider (Claude/OpenAI/Gemini) → creates a DRAFT. Build phased: (a) API key = paid metered API, NOT the Claude subscription, NOT OAuth — Anthropic first; (b) plugin MVP one provider + cron + topic queue; (c) multi-provider routing + featured images; (d) optional auto-publish per lane once trusted.
6. **Produce the first Lane A video** (from the idea bank) so post 1 can publish with a real YouTube embed.
7. **Recipe Shorts pilot** with an AI avatar (sparked by a "detox smoothie" test) → the Recipes category.

## Safety posture (unchanged)

Supervised drafting/reporting. Nothing publishes itself (push-draft forces draft; the plugin should too, initially). Claude does not hold Jonathan's passwords/API keys in chat — credentials live in git-ignored files on his side; Claude writes code, Jonathan runs it. No submitting affiliate applications, no changing accounts.

## Important paths

- Action plan (read first): `06_Planning/ACTION_PLAN_2026-07.md`
- Voice team + schedule: `05_Content/blog/authors-and-schedule.md`
- Categories: `05_Content/blog/categories.md`
- Push-draft tool: `07_Deploy/tools/push-draft.ps1` + `07_Deploy/targets/wordpress/config.example.json`
- Brand child theme: `03_Website/wordpress/themes/manifestedfit-blog/`
- Flat funnel site (root): `03_Website/public/` (brand tokens in `assets/css/styles.css`)
- Affiliate offer registry (still placeholders): `03_Website/public/assets/js/affiliate-offers.js`
- Weekly report task output: `06_Planning/reports/`
- Task list source of truth for follow-ups: `Brain/TASKS.md`

## Still-relevant older context

Affiliate offers remain placeholders (Mindvalley is aspirational — Impact-based, ~30% commission, high ~75k-follower bar; don't gate launch on it). Email capture on the flat site still writes to CSV; deciding on a real email platform (MailerLite/Beehiiv/ConvertKit) is still open. `Archive/Pre_2026-05-01/` holds reusable old scripts/videos.
